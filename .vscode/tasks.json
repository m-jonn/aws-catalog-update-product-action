{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "type": "npm",
            "script": "package:debug",
            "options": {
                "cwd": "${workspaceFolder}"
              }
        }
    ]
}